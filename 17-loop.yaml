- name : looping
  hosts : local
  connection : local
  tasks : 
  - name : print names
    ansible.builtin.debug:
      msg: "Hello {{item}}"
    lopp:
    - ramesh
    - suresh
    - naresh
    - mahesh

